# Tell Splunk to mount the web UI at /splunk/ so nginx can proxy it at that path.
# Without this, all internal links (JS, CSS, API calls) would use absolute paths
# starting at / and break when served behind a reverse proxy sub-path.

[settings]
root_endpoint       = /splunk
enableSplunkWebSSL  = false
