# Production overlay — included for prod only.
# Usage (directly):  docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# Usage (via make):  make up ENV=prod
#
# Cloudflare tunnel → ~/auth-service  (make restart-cloudflared)
# Splunk + Fluent Bit → ~/splunk-service  (make splunk-up)

networks:
  society_net:
    name: ${COMPOSE_PROJECT_NAME:-society}_network
